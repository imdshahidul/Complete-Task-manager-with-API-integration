import 'package:flutter/material.dart';

class CenteredCircularProgInd extends StatelessWidget {
  const CenteredCircularProgInd({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator()
    );
  }
}
